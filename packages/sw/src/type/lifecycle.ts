export enum Lifecycle {
  onInstall = 'onInstall',
  onUninstall = 'onUninstall',
  onActivate = 'onActivate',
  onMessage = 'onMessage',
  onFetch = 'onFetch',
  onSync = 'onSync',
  onPush = 'onPush',
}
